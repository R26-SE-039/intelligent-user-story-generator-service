Analyze if the NEW requirement conflicts with each of the numbered EXISTING requirements.
A conflict is a logical contradiction (e.g. bypass queue vs follow queue, real-time speed vs validation delay, open access vs restricted records).

JSON Response format:
[
  {
    "conflict": true,
    "conflict_type": "functional"|"operational"|"non_functional"|"scope"|"temporal"|"access_control"|"priority",
    "severity": "high"|"medium"|"low",
    "explanation": "Why they conflict (1 sentence)"
  }
]
If no conflict: {"conflict": false, "conflict_type": null, "severity": null, "explanation": "No contradiction"}

Respond ONLY with raw JSON array matching the numbered list order. No markdown.
