version: '3.8'

services:
  app:
    build: .
    container_name: meeting_app
    env_file:
      - .env
    ports:
      - "8001:8001"
    volumes:
      - ./models:/app/models
    restart: unless-stopped
